apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledgerwriter
spec:
  selector:
    matchLabels:
      app: ledgerwriter
  template:
    metadata:
      labels:
        app: ledgerwriter
    spec:
      serviceAccountName: default
      containers:
        - name: ledgerwriter
          image: "{{ .Values.acrLoginServer }}/ledgerwriter:{{ .Values.imageTag }}"
          imagePullPolicy: Always
          envFrom:
            - configMapRef: { name: environment-config }
            - configMapRef: { name: ledger-db-config }
            - configMapRef: { name: service-api-config }
          env:
            - { name: PORT, value: "8080" }
            - { name: ENABLE_TRACING, value: "false" }
            - { name: ENABLE_METRICS, value: "false" }
            - { name: LOG_LEVEL, value: "info" }
            - { name: VERSION, value: "dev" }
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: keys
              mountPath: /root/.ssh
              readOnly: true
            # - { name: publickey, mountPath: /root/.ssh, readOnly: true }
      volumes:
          - name: keys
            csi:
              driver: secrets-store.csi.k8s.io
              readOnly: true
              volumeAttributes:
                secretProviderClass: jwt-key-provider
        # - name: publickey
        #   secret:
        #     secretName: jwt-key
        #     items:
        #       - { key: jwtRS256.key.pub, path: publickey }
---
apiVersion: v1
kind: Service
metadata:
  name: ledgerwriter
spec:
  type: ClusterIP
  selector:
    app: ledgerwriter
  ports:
    - port: 8080
      targetPort: 8080